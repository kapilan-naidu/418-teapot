// controls.js
// Randomly generates a control surface for each audience member
// Wired in next session

function generateControls() {
  // Will pick a random subset of available parameter controls
  // and render them into #control-surface
}
